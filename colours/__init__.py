# add import lines like so - one for each module
# makes import more intuitive for end-user
# allows them to access modules afterwards, like:
#   import colours
#   x = colours.foo.func()
import colours.cli as cli
